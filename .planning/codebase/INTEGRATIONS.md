# External Integrations

**Analysis Date:** 2026-01-12

## APIs & External Services

**Payment Processing:**
- Not detected

**Email/SMS:**
- Not detected

**External APIs:**
- Not detected - Application is self-contained fingerprint management tool

## Data Storage

**Databases:**
- Cloudflare D1 (SQLite) - Primary database
  - Connection: Via Cloudflare Workers environment binding
  - Client: Prisma ORM with `@prisma/adapter-d1`
  - Schema: `services/FE/prisma/schema.prisma`
  - Migrations: `services/FE/scripts/db-migrate.ts`

**File Storage:**
- Not detected - No file upload functionality

**Caching:**
- TanStack React Query - Client-side query caching
  - Configuration: `services/FE/src/trpc/query-client.ts`
  - No server-side caching (Redis, etc.)

## Authentication & Identity

**Auth Provider:**
- Not currently implemented
- `@auth/prisma-adapter` installed but unused
- All routes are public (`publicProcedure`)

**OAuth Integrations:**
- Not detected

## Monitoring & Observability

**Error Tracking:**
- Not detected (no Sentry, Datadog, etc.)

**Analytics:**
- Not detected

**Logs:**
- Console logging in development
- tRPC timing middleware logs procedure execution time
- Location: `services/FE/src/server/api/trpc.ts`

## CI/CD & Deployment

**Hosting:**
- Cloudflare Workers - Primary deployment target
  - Configuration: `services/FE/open-next.config.ts`
  - Adapter: `@opennextjs/cloudflare`
  - Types: `services/FE/cloudflare-env.d.ts` (auto-generated by Wrangler)

**CI Pipeline:**
- Not detected in repository root
- Playwright has GitHub Actions workflows in `playwright/.github/workflows/`

## Environment Configuration

**Development:**
- Required env vars:
  - `DATABASE_URL` - SQLite connection string
  - `NEXT_PUBLIC_BASE_URL` - Public app URL
- Secrets location: `.env` file (gitignored)
- Template: `.env.example` in `services/FE/`

**Staging:**
- Not configured separately

**Production:**
- Cloudflare Workers environment bindings
- D1 database binding: `env.db`
- Assets binding: `env.ASSETS`

**Environment Variable Schema:**
```javascript
// services/FE/src/env.js
server: {
  NODE_ENV: z.enum(["development", "test", "production"]).default("development"),
  VERCEL_URL: z.string().optional(),
  PORT: z.string().optional(),
  DATABASE_URL: z.string(),
}
client: {
  NEXT_PUBLIC_BASE_URL: z.string().url(),
}
```

## Webhooks & Callbacks

**Incoming:**
- Not detected

**Outgoing:**
- Not detected

## Third-Party Libraries

**API Framework:**
- tRPC 11 - Type-safe RPC
  - Server: `services/FE/src/server/api/`
  - Client: `services/FE/src/trpc/react.tsx`
  - HTTP handler: `services/FE/src/app/api/trpc/[trpc]/route.ts`

**ORM:**
- Prisma 7 with D1 adapter
  - Schema: `services/FE/prisma/schema.prisma`
  - Client: `services/FE/src/server/db.ts`
  - Adapters: `@prisma/adapter-d1`, `@prisma/adapter-libsql`

**Query Management:**
- TanStack React Query 5
  - Integration: `services/FE/src/trpc/react.tsx`
  - Client config: `services/FE/src/trpc/query-client.ts`

**Validation:**
- Zod - Schema validation
  - Used in tRPC procedure inputs
  - Environment validation in `src/env.js`

**Browser Extension:**
- WXT - Web Extension Toolkit
  - Config: `services/canvas-extension/wxt.config.ts`
  - Entrypoints: `services/canvas-extension/entrypoints/`

## Configuration Files

**Cloudflare:**
- `services/FE/cloudflare-env.d.ts` - Generated type definitions
- `services/FE/open-next.config.ts` - OpenNext Cloudflare adapter config
- `services/FE/wrangler.jsonc` - Wrangler configuration (if present)

**Database:**
- `services/FE/prisma/schema.prisma` - Prisma schema
- `services/FE/scripts/db-migrate.ts` - Migration script
- `services/FE/scripts/db-generate.ts` - Client generation

**Build:**
- `services/FE/next.config.js` - Next.js configuration
- `services/FE/tsconfig.json` - TypeScript configuration
- `services/FE/package.json` - Dependencies and scripts

---

*Integration audit: 2026-01-12*
*Update when adding/removing external services*
